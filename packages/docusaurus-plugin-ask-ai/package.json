{
  "name": "docusaurus-plugin-ask-ai",
  "version": "0.1.0",
  "description": "Docusaurus plugin that generates llms.txt and per-page markdown files",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "for f in tests/test-*.js; do node \"$f\" || exit 1; done"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "minimatch": "^9.0.3",
    "yaml": "^2.8.1"
  },
  "peerDependencies": {
    "@docusaurus/core": "^3.0.0"
  },
  "devDependencies": {
    "@docusaurus/types": "^3.0.0",
    "@types/minimatch": "^5.1.2",
    "@types/node": "^20.6.0",
    "typescript": "^5.2.2"
  },
  "engines": { "node": ">=18.0" },
  "license": "MIT"
}
